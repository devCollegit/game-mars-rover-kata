# PLAN

## 대상
- 게임 테마 선택 기능을 구현한다. 사용자가 `우주선(SpaceX)`, `공룡`, `아기상어` 세 가지 테마 중 하나를 골라 화면 전체의 분위기와 캐릭터를 바꿀 수 있다.

## 구현 목표

### 1. 테마 구조 설계
- `index.html`의 JS에 `THEMES` 설정 객체를 만들고, 테마 하나가 아래 요소를 정의하도록 한다:
  - `label`: 테마 이름 (우주선 / 공룡 / 아기상어)
  - `colors`: CSS 변수 세트 (배경, 포인트 컬러, 맵 셀 컬러 — 미탐사/탐사완료/장애물)
  - `character`: 맵에서 움직이는 캐릭터의 인라인 SVG (방향 회전, 셀 크기 비례 등 기존 로버 동작 그대로 사용)
  - `copy`: 화면 문구 (타이틀, 부제, 버튼 라벨, 범례 명칭, 오버레이 성공/실패 문구)
  - `background`: 배경 연출 (별밤 / 정글 / 바닷속)
- 테마 적용은 `<html>`(또는 `body`)에 `data-theme` 속성을 지정하고, CSS는 `[data-theme="dino"] { --accent: ...; }` 방식으로 변수만 덮어써서 구현한다. 레이아웃·게임 로직은 테마와 무관하게 공유한다.

### 2. 세 가지 테마 정의
1. **우주선 (기본값, 현재 디자인 유지)**
   - 현재의 SpaceX 다크 테마를 그대로 `space` 테마로 정리한다.
   - 캐릭터: 현재 Starship SVG. 장애물: 암석. 배경: 별.
2. **공룡**
   - 색: 정글 그린 + 화산 오렌지 포인트, 셀은 흙/풀 톤.
   - 캐릭터: 티라노사우루스 SVG (움직이는 주인공).
   - 장애물 칸: 화산암/트리케라톱스 등 다른 공룡 실루엣으로 표현해 "공룡들" 느낌을 살린다.
   - 문구 예: 타이틀 "DINO EXPLORER", 성공 "서식지 탐험 완료!", 실패 "체력이 다 떨어졌어요".
   - 연료 게이지 → "체력"으로 라벨 변경.
3. **아기상어**
   - 색: 밝은 바다 블루 + 노랑 포인트, 셀은 바닷속 톤 (이 테마만 밝은 배경 허용).
   - 캐릭터: 아기상어 SVG (노란 몸통의 귀여운 상어).
   - 장애물 칸: 산호/바위 표현. 배경: 물방울 버블.
   - 문구 예: 타이틀 "BABY SHARK ADVENTURE", 성공 "바다 탐험 성공! 뚜루루뚜루", 실패 "숨이 차서 집에 갔어요".
   - 연료 게이지 → "산소"로 라벨 변경.
   - 주의: 핑크퐁의 아기상어 원곡 캐릭터 이미지·음원은 저작권이 있으므로 사용하지 않는다. 일반적인 귀여운 상어 SVG를 직접 그린다.

### 3. 테마 선택 UI
- 상단 탑바에 테마 선택 버튼 그룹(또는 셀렉트 박스)을 추가한다. 세 테마의 대표 아이콘과 이름을 표시한다.
- 테마를 바꾸면 즉시(새로고침 없이) 색·캐릭터·문구·배경이 전환된다.
  - 진행 중인 게임 상태(맵, 위치, 연료)는 유지된 채 겉모습만 바뀌어야 한다.
  - 맵 위 캐릭터는 현재 위치에서 새 테마의 SVG로 교체된다.
- 선택한 테마를 `localStorage`에 저장하고, 다음 접속 시 자동 적용한다.

### 4. 기존 기능 유지
- 게임 로직(이동, 연료 +1/+10, 목표, 미션 판정)과 백엔드(`main.py`)는 수정하지 않는다.
- 키보드 조종, 미션 오버레이, 연료 경고(70 이상), 충돌 흔들림, 한 화면 레이아웃은 세 테마 모두에서 동일하게 동작해야 한다.
- 연료 경고 색상은 테마별 배색과 어울리게 조정하되 "경고"임이 분명해야 한다.

## 작업 순서
1. 현재 CSS의 색·문구 하드코딩을 CSS 변수/`data-theme` 구조로 정리한다. (space 테마로 추출)
2. `THEMES` 객체와 `applyTheme(themeKey)` 함수를 구현한다. (변수 적용, 캐릭터 교체, 문구/라벨/범례 갱신, localStorage 저장)
3. 공룡·아기상어 테마의 색상 세트와 캐릭터/장애물 SVG를 제작한다.
4. 탑바에 테마 선택 UI를 추가하고 클릭 시 `applyTheme`를 호출한다.
5. 시작 시 localStorage의 테마를 읽어 적용한다. (없으면 space)

## 확인 포인트
- 테마 버튼을 누르면 새로고침 없이 색상, 캐릭터, 타이틀/문구, 범례, 배경이 한 번에 바뀌어야 한다.
- 게임 진행 중 테마를 바꿔도 맵/위치/연료/진행도가 유지되어야 한다.
- 작은 셀 크기에서도 티라노·아기상어 캐릭터의 형태가 식별 가능해야 한다.
- 각 테마에서 미탐사/탐사완료/장애물/캐릭터가 뚜렷이 구분되어야 한다.
- 새로고침 후에도 마지막 선택 테마가 유지되어야 한다.
- 키보드 조종, 오버레이, 연료(체력/산소) 경고, 충돌 흔들림이 세 테마 모두에서 동작해야 한다.
- `main.py`는 변경되지 않아야 한다.
