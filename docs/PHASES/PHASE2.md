## Phase 2: 방향키 명령 입력 후 이동

목표
- 방향키 명령을 입력하고 전송하면 우주선이 이동한다.
- 충돌 여부에 따라 결과를 반환한다.

완료 조건
- 프론트엔드에 상, 하, 좌, 우 버튼과 명령 입력창이 있다.
- `전송` 버튼으로 방향키 명령 배열을 백엔드에 전달한다.
- 백엔드는 명령 순서대로 우주선을 한 칸씩 이동시킨다.
- 충돌이 없으면 `Pass`를 반환한다.
- 벽에 충돌하면 이동을 중단하고 `Warning`을 반환한다.
